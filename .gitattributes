tests/fixtures/news-contract-v1.sample.json -text

# The three per-file LFS rules that used to stand here (plan-360:
# DemoRobotIK.glb, EuropalletEmpty.glb, tests.glb) are GONE, and so are their
# files: plan-395 moved every internal model and fixture into the private
# sibling's projects/Development/, where a blanket `*.glb` LFS rule covers
# them. Nothing large enough to need LFS is tracked here any more - and
# deliberately so, since this repo is mirrored to a public GitHub remote whose
# LFS free tier allows 1 GB of bandwidth a month. If a new large model ever
# has to ship publicly, add its path here explicitly rather than a blanket
# rule: ~198 MB of GLBs are tracked as plain blobs and must stay that way.
