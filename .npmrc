# @nvidia packages (omniverse-webrtc-streaming-library) are NOT on public npm —
# they are served from NVIDIA's Artifactory registry (no auth required).
@nvidia:registry=https://edge.urm.nvidia.com/artifactory/api/npm/omniverse-client-npm/
